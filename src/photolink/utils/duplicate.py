# does not work.
# if __name__ == '__main__':
#     from imagededup.methods import PHash
#     phasher = PHash()
#     encodings = phasher.encode_images(image_dir='/Users/philipcho/photomatcher/sample')
#     duplicates = phasher.find_duplicates(encoding_map=encodings)

#     print(duplicates)
